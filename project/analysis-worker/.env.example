# Server Configuration
PORT=8080

# Note: IronDrive is just a file picker (like Dropbox or PC upload)
# It does not provide any API services for barcode analysis
# All barcode scanning happens in this worker using ZXing and jsQR
